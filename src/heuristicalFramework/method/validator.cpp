#include "validator.h"

/*
            constructor
*/
template <typename Tsol, typename Tchange>
Validator<Tsol, Tchange>::Validator() { NotImplemented(); };

/*
            debug functions
*/
template <typename Tsol, typename Tchange>
void Validator<Tsol, Tchange>::debugCmd() { NotImplemented(); };
template <typename Tsol, typename Tchange>
string Validator<Tsol, Tchange>::debugStr() { NotImplemented(); };

/*
            documentation functions
*/
template <typename Tsol, typename Tchange>
void Validator<Tsol, Tchange>::docCmd() { NotImplemented(); };
template <typename Tsol, typename Tchange>
string Validator<Tsol, Tchange>::docStr() { NotImplemented(); };