#include "moveGen.h"

#include "../notImplemented.h"
#include <vector>

using namespace std;

template <typename Tsol, typename Tchange>
MoveGen<Tsol, Tchange>::MoveGen()
{
    NotImplemented();
};

template <typename Tsol, typename Tchange>
bool MoveGen<Tsol, Tchange>::generate() { NotImplemented(); };

template <typename Tsol, typename Tchange>
void MoveGen<Tsol, Tchange>::printCmd() { NotImplemented(); };
template <typename Tsol, typename Tchange>
void MoveGen<Tsol, Tchange>::explainCmd() { NotImplemented(); };