#include "moveGen.h"

#include "../notImplemented.h"

#include <string>
#include <vector>

using namespace std;

/*
            constructor
*/
template <typename Tsol, typename Tchange>
MoveGen<Tsol, Tchange>::MoveGen()
{
    NotImplemented();
};

/*
            essential functions
*/
template <typename Tsol, typename Tchange>
bool MoveGen<Tsol, Tchange>::generate() { NotImplemented(); };

/*
            debug functions
*/
template <typename Tsol, typename Tchange>
void MoveGen<Tsol, Tchange>::debugCmd() { NotImplemented(); };
template <typename Tsol, typename Tchange>
string MoveGen<Tsol, Tchange>::debugStr() { NotImplemented(); };

/*
            documentation functions
*/
template <typename Tsol, typename Tchange>
void MoveGen<Tsol, Tchange>::docCmd() { NotImplemented(); };
template <typename Tsol, typename Tchange>
string MoveGen<Tsol, Tchange>::docStr() { NotImplemented(); };